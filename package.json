{
  "name": "wled-video-stream",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Stream a phone camera to any WLED 2D matrix over DDP.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "check": "node --check src/*.js && node --check public/*.js"
  },
  "dependencies": {
    "qrcode": "^1.5.4",
    "selfsigned": "^3.0.1",
    "ws": "^8.18.3"
  }
}
